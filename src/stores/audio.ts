import {
    defineStore,
} from 'pinia';

const useAudioStore = defineStore({
    id: 'audioStore',
    state() {
        return {
          
        }
    },
    getters: {
 
    },
    actions: {

      

    }
})

export default useAudioStore;